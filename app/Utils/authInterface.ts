export type registerType = {
    courseId: string;
    email: string;
    username: string;
    password: string;
    confirmPassword: string;
}

export type LoginType = {
    username: string;
    password: string;
}